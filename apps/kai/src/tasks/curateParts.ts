// apps/kai/src/tasks/curateParts.ts

/**********************************************************************
 * Phase-3 placeholder
 * -------------------
 *  • Daily cron (src/api/cron/curate-parts.ts) calls this function.
 *  • Intended to scan open tickets, pull `partsNeeded` custom field,
 *    deduplicate, then write JSON or push to dashboard.
 *
 *  For now it simply logs a heartbeat so the cron job has something
 *  to await.  Stub is kept in a separate file so real logic can
 *  replace it without touching the API route.
 *********************************************************************/

export async function curateParts() {
    console.log('[curateParts] stub – nothing to do yet');
  }
  